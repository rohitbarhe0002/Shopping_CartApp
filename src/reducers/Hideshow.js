import { SHOW_HIDE } from "../constants/ActionTypes";
const showhide = (state = initialState.addedIds, action) => {
    switch (action.type) {
      case SHOW_HIDE:
     {
          return state
          
        }
    
      default:
        return state
    }
  }
  